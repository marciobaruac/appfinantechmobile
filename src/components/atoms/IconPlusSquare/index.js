import React from 'react'
import Icon from 'react-native-vector-icons/Feather';

export const IconPlusSquare = ({ size }) => {
    return (
        <Icon name='plus-square' style={{ fontSize: 20 }} />
    )
}